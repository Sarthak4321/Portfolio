<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Message Submitted</title>
  <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
  <script src="https://unpkg.com/@lottiefiles/lottie-player@latest/dist/lottie-player.js"></script>
  <style>
    @keyframes pop {
      0% { transform: scale(0.8); opacity: 0; }
      100% { transform: scale(1); opacity: 1; }
    }

    @keyframes confetti {
      0% { transform: translateY(0) rotate(0); opacity: 1; }
      100% { transform: translateY(200px) rotate(720deg); opacity: 0; }
    }

    .pop-animation {
      animation: pop 0.5s ease-out forwards;
    }

    .confetti {
      position: absolute;
      width: 8px;
      height: 8px;
      background: black;
      animation: confetti 2s ease-in forwards;
      opacity: 0;
    }
  </style>
</head>
<body class="bg-white text-black flex items-center justify-center min-h-screen px-4 overflow-hidden">

  <div class="max-w-md w-full text-center border border-black p-8 rounded-2xl shadow-lg pop-animation relative">
    <!-- Confetti Effect -->
    <div class="confetti" style="top: 10px; left: 20px; animation-delay: 0s;"></div>
    <div class="confetti" style="top: 10px; left: 100px; animation-delay: 0.2s;"></div>
    <div class="confetti" style="top: 10px; left: 180px; animation-delay: 0.4s;"></div>
    <div class="confetti" style="top: 10px; left: 260px; animation-delay: 0.6s;"></div>
    <div class="confetti" style="top: 10px; left: 340px; animation-delay: 0.8s;"></div>

    <div class="flex justify-center mb-4">
      <lottie-player
        src="https://assets5.lottiefiles.com/packages/lf20_jbrw3hcz.json"
        background="transparent"
        speed="1"
        style="width: 120px; height: 120px;"
        autoplay
      ></lottie-player>
    </div>

    <h1 class="text-2xl font-bold mb-2">Message Sent Successfully!</h1>
    <p class="text-gray-700 mb-6">Thank you for reaching out. We'll get back to you shortly.</p>
    <a href="/" class="inline-block px-6 py-2 border border-black rounded-full text-sm font-semibold transition hover:bg-black hover:text-white">
      Go Back Home
    </a>
  </div>

</body>
</html>